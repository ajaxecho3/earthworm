# Reddit Official API Credentials
# Get these by creating a Reddit app at: https://www.reddit.com/prefs/apps

# Required for Reddit Official API
REDDIT_CLIENT_ID=your_reddit_client_id_here
REDDIT_CLIENT_SECRET=your_reddit_client_secret_here

# Optional - Basic Configuration
REDDIT_USER_AGENT=Earthworm Data Collector 1.0
REDDIT_USERNAME=your_reddit_username
REDDIT_PASSWORD=your_reddit_password

# Anti-Bot Detection & Rate Limiting Configuration (Optional)
# These settings help avoid bot detection and rate limiting

# Retry and delay settings
REDDIT_MAX_RETRIES=3                    # Maximum retry attempts on rate limit
REDDIT_BASE_DELAY=2.0                   # Base delay between retries (seconds)
REDDIT_REQUEST_DELAY=0.5                # Minimum delay between requests (seconds)

# Human-like behavior settings
REDDIT_RANDOM_DELAYS=True               # Add random jitter to delays
REDDIT_MIN_JITTER=0.3                   # Minimum jitter multiplier
REDDIT_MAX_JITTER=1.2                   # Maximum jitter multiplier

# Burst protection settings
REDDIT_BURST_PROTECTION=True            # Enable burst protection
REDDIT_MAX_REQUESTS_PER_MINUTE=30       # Maximum requests per minute

# Application settings
REDDIT_TIMEOUT=30                       # Request timeout (seconds)

# Instructions:
# 1. Go to https://www.reddit.com/prefs/apps
# 2. Click "Create App" or "Create Another App"
# 3. Choose "script" as the app type
# 4. Copy the client ID (under the app name)
# 5. Copy the client secret
# 6. Fill in the values above and rename this file to .env
# 7. For stealth mode, keep all anti-bot settings enabled
